import { Route } from '@angular/router';
import { DeviceDetailComponent } from './device-detail';

export const deviceDetailRoutes: Route[] = [
  {
    path: '',
    component: DeviceDetailComponent
  }
];
